# 🔐 Flutter Clean Auth

A Flutter authentication app built with **Clean Architecture**, **BLoC**, **GoRouter**, and **GetIt**.  
Includes Splash, Login, and Home screen.

**Test credentials:** `akash@turvaldx.com` / `akash@2001`

```bash
flutter pub get && flutter run
```
